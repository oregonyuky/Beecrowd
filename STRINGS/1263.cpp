#include <bits/stdc++.h>
#define pb push_back
using namespace std;
vector<string> fun(string st){
    vector<string> s1;
    stringstream ss(st);
    string wo;
    while(ss >> wo){
        s1.pb(wo);
    }
    return s1;
}
int func(vector<string> v1){
    while(pos<TL){
        wihlf
    }
}
int main(){
    string st;
    while(getline(cin, st)){
        vector<string> st1 = fun(st);
        int cont=0;
        int pos=0;
        for(int i=0;i<)
        cout << cont << "\n";
    }
    return 0;
}